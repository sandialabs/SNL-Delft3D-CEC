import bmi.wrapper

# really nothing to do here....
# You might want to do something like:
# But that only works if engine is found in path
# class DFlowFM(bmi.wrapper.BMIWrapper):
#     engine = "dflowfm"
#     def __init__(self, *args, **kwargs):
#         # use engine default
#         kwargs.setdefault('engine', this.engine)
#         super(bmi.wrapper.BMIWrapper, self).__init__(*args, **kwargs)
