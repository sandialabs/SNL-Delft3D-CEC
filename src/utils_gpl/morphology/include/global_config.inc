!! EMPTY HEADER FILE to serve as fallback when no global_config.inc
!! is found in the solution's/package's root directory.
!!
!! currently available macros:
!! MOR_USE_ECMODULE - define this macro (<>0) to enable EC-module for space-varying input in sed/mor.
